# Compiled binaries (anchored to repo root — bare names would otherwise also
# match cmd/tukan/ and cmd/tukan-server/, silently excluding those packages)
/tukan
/tukan.exe
/tukan-server
/tukan-server.exe
*.exe~

# Go build cache
*.test

# Database files (user data — not committed)
*.db
*.db-wal
*.db-shm

# Local Claude Code settings
.claude/
